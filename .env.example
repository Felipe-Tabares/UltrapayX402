# URL del API Gateway de AWS Lambda (Backend)
# En desarrollo local usar: http://localhost:3000
# En producción usar el output de Terraform: https://xxx.execute-api.us-east-1.amazonaws.com/dev
VITE_API_URL=http://localhost:3000

# Modo mock: usar datos simulados sin backend real
# Cambiar a 'false' cuando el backend esté desplegado en Lambda
VITE_USE_MOCK=true

# Red de blockchain para x402 (base-sepolia para testnet)
VITE_X402_NETWORK=base-sepolia
